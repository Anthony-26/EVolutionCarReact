import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  // StictMode Disable to render pages only once
  // <React.StrictMode>
    <App/>
  // </React.StrictMode>
)
